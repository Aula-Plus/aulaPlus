# Base URL of the Laravel API. The SPA talks to it with credentials (cookies),
# so this must match the Sanctum stateful domain + CORS allowed origin on the API.
# Local dev (Sail serves the API on port 80):
VITE_API_URL=http://localhost
